ARG DOCKER_FRONTEND_PREIMAGE_NAME
ARG FRONTEND_PORT

FROM $DOCKER_FRONTEND_PREIMAGE_NAME:latest

WORKDIR container/

COPY . .

EXPOSE $FRONTEND_PORT

CMD streamlit run app.py --server.port $FRONTEND_PORT

